build:
    tests:
        override:
            -
                command: vendor/bin/phpunit --coverage-clover 'builds/clover.xml'
                coverage:
                    file: 'builds/clover.xml'
                    format: 'clover'
